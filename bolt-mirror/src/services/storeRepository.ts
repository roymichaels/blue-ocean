export {
  StoreRepository,
  type StoreRepositoryEventMap,
  type StoreRepositoryDiff,
  type SaveStoreResult,
  createStoreRepository,
  createDefaultStoreRepositoryDeps,
  storeRepository,
} from '@/features/stores/services/storeRepository';

export { storeRepository as default } from '@/features/stores/services/storeRepository';
