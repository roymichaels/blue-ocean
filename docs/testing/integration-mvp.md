# Integration Tests (MVP)

- Store creation: success/failure (offline/online)
- Checkout: with/without `checkout` scope; PIN required
- Admin bootstrap: first wallet auto-admin; approvals work
- Warm cache parity: cold vs warm
